namespace Synapse.RCE.Models
{
    public enum RceStatus
    {
        Success,
        CompilationFailed,
        RunFailed,
        AssemblyAlreadyLoaded,
        InvalidJson,
        ConnectionFailed
    }
}
